import * as THREE from './vendor/three.module.min.js';
(function(){'use strict';var canvas=document.getElementById('zaec404Canvas'),rootEl=document.querySelector('.zaec-404');if(!canvas||!rootEl)return;var reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,coarse=matchMedia('(pointer: coarse)').matches;var renderer;try{renderer=new THREE.WebGLRenderer({canvas:canvas,alpha:true,antialias:!coarse,powerPreference:'high-performance'});}catch(e){document.documentElement.classList.add('no-webgl');return;}rootEl.classList.add('has-webgl');renderer.setPixelRatio(Math.min(devicePixelRatio||1,coarse?1.15:1.65));renderer.setClearColor(0x0e0e0d,0);var scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.1,100);camera.position.set(7.5,5.4,10.5);camera.lookAt(0,.8,0);var group=new THREE.Group();scene.add(group);var mat=new THREE.LineBasicMaterial({color:0x7dd3ff,transparent:true,opacity:.5,blending:THREE.AdditiveBlending});var dim=new THREE.LineBasicMaterial({color:0x7dd3ff,transparent:true,opacity:.13});function wireBox(x,y,z,w,h,d,m){var geo=new THREE.EdgesGeometry(new THREE.BoxGeometry(w,h,d));var line=new THREE.LineSegments(geo,m||mat);line.position.set(x,y,z);group.add(line);return line;}wireBox(0,-1.5,0,6.5,.08,5.2,dim);wireBox(-2.7,.2,0,.08,3.4,5.2);wireBox(2.7,.2,0,.08,3.4,5.2);wireBox(0,.2,-2.55,5.4,3.4,.08);wireBox(-1.55,.25,2.55,2.3,3.3,.08);wireBox(1.85,.4,2.55,1.6,3,.08);wireBox(-.8,-.05,2.57,1.2,2.25,.04,dim);wireBox(1.85,.45,2.58,.9,1.2,.04,dim);var roofA=new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(-2.8,1.9,-2.65),new THREE.Vector3(0,4.1,-2.65),new THREE.Vector3(2.8,1.9,-2.65),new THREE.Vector3(-2.8,1.9,-2.65),new THREE.Vector3(-2.8,1.9,2.65),new THREE.Vector3(0,4.1,2.65),new THREE.Vector3(2.8,1.9,2.65),new THREE.Vector3(2.8,1.9,-2.65),new THREE.Vector3(0,4.1,-2.65),new THREE.Vector3(0,4.1,2.65)]);group.add(new THREE.Line(roofA,mat));var missing=wireBox(.35,1.45,2.8,1.1,1.1,1.1,new THREE.LineDashedMaterial({color:0x7dd3ff,dashSize:.12,gapSize:.1,transparent:true,opacity:.8}));missing.computeLineDistances();var frags=[];for(var i=0;i<(coarse?12:28);i++){var f=wireBox((Math.random()-.5)*10,(Math.random()-.3)*6,(Math.random()-.5)*7,.12+Math.random()*.3,.12+Math.random()*.3,.12+Math.random()*.3,dim);f.userData={s:.15+Math.random()*.3,p:Math.random()*6.28};frags.push(f);}var pg=new THREE.BufferGeometry(),count=coarse?70:150,pos=new Float32Array(count*3);for(i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*14;pos[i*3+1]=(Math.random()-.3)*9;pos[i*3+2]=(Math.random()-.5)*10;}pg.setAttribute('position',new THREE.BufferAttribute(pos,3));scene.add(new THREE.Points(pg,new THREE.PointsMaterial({color:0x7dd3ff,size:2,sizeAttenuation:false,transparent:true,opacity:.3})));var pointer={x:0,y:0},target={x:0,y:0};if(!reduce&&!coarse){window.addEventListener('pointermove',function(e){target.x=(e.clientX/innerWidth-.5)*.22;target.y=(e.clientY/innerHeight-.5)*.12;},{passive:true});}function resize(){var w=rootEl.clientWidth,h=rootEl.clientHeight;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix();}resize();window.addEventListener('resize',resize,{passive:true});var last=performance.now();function frame(now){var dt=Math.min((now-last)/1000,.05);last=now;pointer.x+=(target.x-pointer.x)*.04;pointer.y+=(target.y-pointer.y)*.04;group.rotation.y=-.45+pointer.x+(reduce?0:Math.sin(now*.00018)*.035);group.rotation.x=.08-pointer.y;missing.rotation.y=reduce?0:now*.00055;missing.position.y=1.45+(reduce?0:Math.sin(now*.0014)*.12);frags.forEach(function(f,j){if(reduce)return;f.rotation.x+=dt*f.userData.s;f.rotation.y+=dt*f.userData.s*.7;f.position.y+=Math.sin(now*.0006+f.userData.p)*.0008;});renderer.render(scene,camera);if(!reduce)requestAnimationFrame(frame);}requestAnimationFrame(frame);})();
